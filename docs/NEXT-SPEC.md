# Next Spec Implementation Guide

## Archive note — 2026-08-12

Spec 07 One-Click Deployment is implemented (frontend, backend, automated
integration, and preview). The real-host acceptance run uses
`examples/oars-react-smoke` on the connected Contabo server. See
`docs/specs/07-deployment.md` and `docs/HANDOVER.md` §36. The previous guide
content is preserved in git history.

## Next target

Stage 6 backlog per `ROADMAP.md` §4: specs **09 (Access Management)**, **08 (SSH Management)**,
and **10 (Backups)** — in that order. Treat each spec file as the product contract.

Read before editing:
- `docs/specs/README.md`
- `docs/DESIGN.md`
- `docs/HANDOVER.md` (esp. §7 pitfalls, §36 last handover)
- the target spec's `docs/specs/<nn>-*.md` and its §13 references
